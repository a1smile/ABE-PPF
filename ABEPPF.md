# 歧义感知与预算自适应 PPF 位姿估计方法：Idea 与实验组织

## 1. 论文整体定位

建议将论文主线从“通过增强检索和复杂后端提高精度”调整为：

> **不是生成更多候选来提高精度，而是识别更有价值的点对、只在必要位置补偿量化误差，并在姿态模式稳定后提前停止无效计算。**

建议中文题目：

> **歧义感知与预算自适应的高效 PPF 位姿配准方法**

建议英文题目：

> **Ambiguity-aware and Budget-adaptive Efficient PPF Registration**

或：

> **Ambiguity-aware and Budget-adaptive PPF for Efficient 6D Pose Estimation**

整体思想是：

> 复杂场景下 PPF 的失败并不只来自候选不足，也来自大量低判别点对、高碰撞哈希桶、重复结构伪投票和不必要的后端验证。本文从点对生成、候选检索和后端模式恢复三个阶段控制计算预算，使 PPF 在保持鲁棒性的同时减少无效匹配与冗余验证。

最终形成三个核心 idea：

1. **歧义感知的高价值场景点对选择**
2. **不确定性感知的边界选择性候选检索**
3. **预算自适应的可信姿态模式恢复**

三者分别对应 PPF pipeline 的三个关键环节：

| 阶段     | 传统/初稿思路                    | 新方法目标                           |
| -------- | -------------------------------- | ------------------------------------ |
| 点对生成 | 尽量多生成场景点对               | 只生成高价值、低歧义点对             |
| 哈希检索 | 固定单桶检索或全量增强检索       | 仅对存在跨 bin 风险的点对补查邻桶    |
| 后端决策 | 所有候选生成后再投票、聚类、验证 | 边生成边评估，姿态模式稳定后提前停止 |

一句话概括：

> **Idea 1 负责少生成，Idea 2 负责少补查，Idea 3 负责早停止。**

------

# 2. Idea 1：歧义感知的高价值场景点对选择

## 2.1 要解决的问题

经典 PPF 在线阶段通常从场景点云中选取参考点，再与其他场景点组成点对。点对数量一旦过多，后续哈希检索、候选生成、投票和几何验证都会明显变慢。

但是，并不是所有场景点对都有同等价值。复杂场景中存在大量低价值点对，例如：

- 大平面区域上的点对：法向稳定，但几何区分度低；
- 圆柱、对称结构、重复结构上的点对：数量多，但容易形成伪匹配；
- 噪声边界点对：法向不稳定，容易落入错误哈希桶；
- 高碰撞 PPF 桶对应的点对：会检索出大量模型点对，增加时间并形成伪投票；
- 同一区域密集采样点对：信息高度冗余。

因此，第一步应该在点对生成阶段就减少无效计算：

> 在形成大量场景点对之前，先判断哪些场景点适合作为参考点，哪些点对值得进入 PPF 检索。

## 2.2 核心思想

高价值点不是简单的“曲率高”或“处于边缘”，而是：

> 一个场景点如果法向稳定、局部几何有一定区分度，并且它与其他点形成的 PPF 点对在模型哈希空间中不容易混淆，那么它就是高价值参考点。

与已有曲率采样或边缘采样的区别在于：

| 方法          | 判断依据                                 | 主要局限                       |
| ------------- | ---------------------------------------- | ------------------------------ |
| 曲率采样      | 曲率越大越显著                           | 曲率大不一定 PPF 匹配唯一      |
| 边缘采样      | 边缘点更有区分度                         | 依赖稳定边缘，噪声边界可能误导 |
| 随机/均匀采样 | 减少点数                                 | 可能丢掉关键结构               |
| 本方法        | PPF 哈希歧义度 + 法向稳定性 + 点对多样性 | 直接针对 PPF 匹配机制          |

## 2.3 高价值点的判断指标

### 2.3.1 法向稳定性

PPF 特征依赖点对距离和法向夹角，因此法向不稳定的点不适合作为参考点。

对场景点 (s_i)，取邻域 (\mathcal{N}(s_i))，可以用邻域法向一致性衡量法向稳定性：

[
Q_n(s_i)=\frac{1}{|\mathcal{N}(s_i)|}\sum_{s_j\in \mathcal{N}(s_i)} |n_i^\top n_j|
]

其中 (n_i) 和 (n_j) 分别表示点 (s_i) 与邻域点 (s_j) 的法向。(Q_n(s_i)) 越大，说明该点邻域法向越一致，点越稳定。

也可以使用邻域 PCA 特征值估计局部表面变化。设协方差矩阵特征值为：

[
\lambda_1 \geq \lambda_2 \geq \lambda_3
]

则局部表面变化量为：

[
C(s_i)=\frac{\lambda_3}{\lambda_1+\lambda_2+\lambda_3}
]

需要注意的是，不能简单选择 (C(s_i)) 最大的点：

- (C(s_i)) 太小：可能位于大平面，区分度低；
- (C(s_i)) 适中：局部结构有一定变化，较有价值；
- (C(s_i)) 太大：可能是噪声、遮挡边界或破碎区域。

因此，高价值点应满足：

> 法向稳定，同时具有适度几何变化。

### 2.3.2 PPF 哈希桶判别力

这是该 idea 的关键创新。

离线阶段，对模型 PPF 哈希表中每个桶进行统计：

[
N(h)=\text{bucket } h \text{ 中的模型点对数量}
]

如果 (N(h)) 很大，说明大量模型点对具有相似 PPF 特征，该桶为高歧义桶。场景点对落入该桶后，会检索出大量模型点对，既耗时，又容易形成伪候选。

因此定义桶判别力：

[
D(h)=\log\frac{N_{\text{all}}}{N(h)+1}
]

其中，(N_{\text{all}}) 表示模型点对总数。

含义如下：

- (D(h)) 大：桶内模型点对少，特征更独特；
- (D(h)) 小：桶内模型点对多，特征更容易混淆。

在线阶段，对于一个候选参考点 (s_r)，先从场景中分层采样少量配对点 (s_j)，计算点对 PPF 特征：

[
g_{rj}=PPF(s_r,s_j)
]

并量化到模型哈希桶：

[
h_{rj}=Q(g_{rj})
]

然后计算该参考点形成点对的平均判别力：

[
A(s_r)=\frac{1}{K}\sum_{j=1}^{K}D(h_{rj})
]

如果一个点作为参考点后，形成的点对大多落入高判别力桶，则该点更适合作为高价值参考点。

### 2.3.3 点对多样性

一个参考点不能只和同一方向、同一距离段或同一法向关系的点形成点对，否则信息仍然冗余。

可以将配对点按照几何关系划分为若干 bin：

- 距离 bin：近、中、远；
- 法向夹角 bin：小角度、中角度、大角度；
- 空间方向 bin：不同方位。

如果一个参考点形成的有效点对覆盖更多 bin，说明该参考点提供的几何约束更加丰富。

可以用熵定义点对多样性：

[
B(s_r)=-\sum_b p_b\log p_b
]

其中 (p_b) 表示该参考点形成的有效点对落入第 (b) 个几何关系 bin 的比例。

### 2.3.4 空间冗余惩罚

即使多个点都具有较高价值，也不能让它们全部集中在同一局部区域，否则候选证据会偏向局部结构。

可以使用 voxel NMS 或 farthest point sampling 保证参考点在空间上分布均匀。

可以定义冗余惩罚项：

[
R(s_r)=\exp\left(-\frac{d(s_r,S_{\text{selected}})^2}{\sigma^2}\right)
]

其中 (d(s_r,S_{\text{selected}})) 表示当前点到已选参考点集合的最近距离。如果 (s_r) 距离已选点太近，则其优先级降低。

## 2.4 综合价值分数

最终可以定义场景参考点价值：

[
V(s_r)=Q_n(s_r)\cdot \left(\alpha A(s_r)+\beta B(s_r)\right)\cdot (1-R(s_r))
]

其中：

- (Q_n(s_r))：法向稳定性；
- (A(s_r))：PPF 哈希判别力；
- (B(s_r))：点对多样性；
- (R(s_r))：空间冗余惩罚；
- (\alpha,\beta)：权重参数。

在线阶段只选择 top-(M) 个参考点进入后续点对生成。

## 2.5 算法流程

```text
离线阶段：
1. 对模型点云构建 PPF 哈希表
2. 统计每个哈希桶 h 中的模型点对数量 N(h)
3. 计算桶判别力 D(h)=log(N_all/(N(h)+1))

在线阶段：
1. 对场景点云估计法向和局部 PCA 特征
2. 对每个场景点 s_r：
      计算法向稳定性 Q_n(s_r)
      若 Q_n 过低，则跳过

      分层采样 K 个配对点 s_j
      对每个点对 (s_r,s_j)：
          计算 PPF 特征 g_rj
          得到哈希桶 h_rj
          查询桶判别力 D(h_rj)

      计算平均 PPF 判别力 A(s_r)
      计算点对多样性 B(s_r)
      计算空间冗余惩罚 R(s_r)
      得到综合价值 V(s_r)

3. 按 V(s_r) 排序
4. 使用空间 NMS 选择 top-M 个高价值参考点
5. 只围绕这些参考点生成场景点对
```

## 2.6 论文表述建议

> 本文提出一种歧义感知的场景参考点与点对选择策略。不同于仅依赖曲率或边缘显著性的采样方法，本文从 PPF 哈希匹配机制出发，利用模型哈希桶碰撞统计估计场景点对的潜在匹配歧义，并结合局部法向稳定性、点对几何多样性和空间冗余约束选择高价值参考点。该策略能够在点对生成阶段减少低判别、重复和不稳定点对，从而降低无效哈希检索和伪投票累积。

------

# 3. Idea 2：不确定性感知的边界选择性候选检索

## 3.1 要解决的问题

经典 PPF 使用固定量化函数将连续 PPF 特征映射到离散哈希桶中。问题在于：

> 如果真实匹配点对的 PPF 特征位于 bin 边界附近，轻微法向噪声、采样误差或深度扰动就可能让它落入相邻桶，导致正确模型点对无法被检索到。

原始的多分辨率或随机移位检索虽然可以提高召回，但容易被理解为：

> 多查表、多检索，用时间换精度。

因此可以改为：

> 只有当点对特征确实存在跨 bin 风险时，才查询邻桶。

该模块可命名为：

> **不确定性感知的边界选择性候选检索**

英文：

> **Uncertainty-aware Boundary Selective Probing, UBSP**

## 3.2 核心思想

判断是否启动邻桶检索，不只是看点对是否靠近 bin 边界，而是看：

> PPF 特征到 bin 边界的距离，是否小于该特征的不确定性范围。

如果满足：

[
\Delta_k < \lambda \sigma_k
]

则说明第 (k) 维特征可能因为扰动跨过 bin 边界，需要查询对应方向的邻桶。

其中：

- (\Delta_k)：第 (k) 维 PPF 特征到最近 bin 边界的距离；
- (\sigma_k)：第 (k) 维 PPF 特征的不确定性；
- (\lambda)：安全系数，可取 (1\sim2)。

这个标准比单纯的“靠近边界就扩展”更严谨，因为它判断的是：

> 特征扰动范围是否可能覆盖到量化边界。

## 3.3 边界距离计算

PPF 特征定义为：

[
g=[d,\alpha,\beta,\gamma]
]

其中：

[
d=|s_i-s_r|
]

[
\alpha=\angle(n_r,s_i-s_r)
]

[
\beta=\angle(n_i,s_i-s_r)
]

[
\gamma=\angle(n_r,n_i)
]

设第 (k) 维量化宽度为 (w_k)，则当前 bin 左右边界为：

[
l_k=\left\lfloor \frac{g_k}{w_k} \right\rfloor w_k
]

[
u_k=l_k+w_k
]

该维到最近边界的距离为：

[
\Delta_k=\min(g_k-l_k,u_k-g_k)
]

(\Delta_k) 越小，说明该维越靠近量化边界。

## 3.4 特征不确定性估计

特征不确定性可以由点位置噪声和法向噪声近似估计。

### 3.4.1 点位置不确定性

对点 (s_i)，用局部 PCA 残差估计：

[
\sigma_p(s_i)=\sqrt{\frac{\lambda_3}{\lambda_1+\lambda_2+\lambda_3}}
]

其中 (\lambda_1,\lambda_2,\lambda_3) 为邻域协方差矩阵特征值。(\lambda_3) 越大，说明局部表面越不稳定或噪声越强。

### 3.4.2 法向不确定性

用邻域法向一致性估计：

[
\sigma_n(s_i)=\arccos\left(
\frac{1}{|\mathcal{N}(s_i)|}
\sum_{s_j\in \mathcal{N}(s_i)}
|n_i^\top n_j|
\right)
]

如果邻域法向越一致，则 (\sigma_n(s_i)) 越小。

## 3.5 PPF 四维不确定性

对于点对 ((s_r,s_i))，距离维不确定性可以近似为：

[
\sigma_d=\sqrt{\sigma_p(s_r)^2+\sigma_p(s_i)^2}
]

连线方向不确定性可以近似为：

[
\sigma_u=\frac{\sqrt{\sigma_p(s_r)^2+\sigma_p(s_i)^2}}{\max(d,\epsilon)}
]

三个角度维的不确定性可近似为：

[
\sigma_\alpha=\sqrt{\sigma_n(s_r)^2+\sigma_u^2}
]

[
\sigma_\beta=\sqrt{\sigma_n(s_i)^2+\sigma_u^2}
]

[
\sigma_\gamma=\sqrt{\sigma_n(s_r)^2+\sigma_n(s_i)^2}
]

于是：

[
\sigma_g=[\sigma_d,\sigma_\alpha,\sigma_\beta,\sigma_\gamma]
]

## 3.6 邻桶检索启动规则

对每一维计算边界风险：

[
\rho_k=\frac{\Delta_k}{\sigma_k+\epsilon}
]

(\rho_k) 越小，表示跨 bin 风险越大。

启动条件为：

[
\Delta_k < \lambda\sigma_k
]

同时，为避免噪声过大的点对触发大量扩展，需要增加不确定性上限：

[
\sigma_k < \sigma_k^{\max}
]

最终启动条件为：

[
\Delta_k < \lambda\sigma_k
\quad \text{and} \quad
\sigma_k < \sigma_k^{\max}
]

含义如下：

- 如果点对稳定且远离边界：只查当前桶；
- 如果点对稳定但靠近边界：查当前桶 + 少量邻桶；
- 如果点对不稳定性过大：不扩展，甚至跳过；
- 如果当前桶本身高歧义：限制扩展，避免候选爆炸。

## 3.7 邻桶扩展方向

如果靠近左边界：

[
g_k-l_k < \lambda\sigma_k
]

则查询左邻桶。

如果靠近右边界：

[
u_k-g_k < \lambda\sigma_k
]

则查询右邻桶。

如果多个维度都满足条件，不建议全组合扩展，否则 4 维邻桶组合会爆炸。可以只选择风险最大的前 (m) 个维度：

[
\rho_k=\frac{\Delta_k}{\sigma_k+\epsilon}
]

选择 (\rho_k) 最小的前 (m) 维进行扩展。

建议：

- (m=1)：速度优先；
- (m=2)：精度与速度折中；
- 不建议 (m=4)：候选数量可能增长过快。

## 3.8 算法流程

```text
对于场景点对 (s_r, s_i)：

1. 计算 PPF 特征 g=[d, α, β, γ]
2. 根据量化宽度 w 得到当前哈希桶 h
3. 计算每一维到 bin 边界的距离 Δ_k
4. 估计每一维特征不确定性 σ_k
5. 计算边界风险 ρ_k=Δ_k/(σ_k+ε)

6. 如果所有维度都满足 ρ_k ≥ λ：
       只查询当前桶 h

7. 如果存在维度满足 Δ_k < λσ_k：
       选择风险最大的前 m 个维度
       查询当前桶 + 对应方向的邻桶

8. 如果点对整体不确定性过大：
       不扩展，或直接跳过该点对

9. 如果当前桶或邻桶是高歧义桶：
       限制返回数量，或降低该点对投票权重
```

## 3.9 论文表述建议

> 本文提出一种不确定性感知的边界选择性候选检索机制。针对固定量化边界导致的正确对应漏检问题，本文不再对所有点对执行多分辨率或随机移位增强检索，而是根据 PPF 连续特征到量化边界的距离及其特征不确定性判断是否存在跨 bin 风险。仅当边界距离小于不确定性范围时，系统才对相应方向的邻接桶执行有限扩展。该机制将量化误差补偿限制在真正可能发生边界跨越的点对上，从而在提高正确匹配召回率的同时避免全量增强检索带来的额外时间开销。

------

# 4. Idea 3：预算自适应的可信姿态模式恢复

## 4.1 要解决的问题

传统 PPF 后端通常是：

1. 所有点对都生成候选；
2. 所有候选都投票；
3. 投票结束后选择最高峰或聚类中心；
4. 再做几何验证或 ICP。

该流程的问题是：

> 即使前面已经出现了一个非常稳定的正确姿态模式，算法也会继续处理大量点对，造成不必要的计算。

另一方面，如果场景遮挡严重、重复结构明显或多峰歧义强，轻量计算又可能不够。

因此需要：

> 简单样本少算，困难样本多算；姿态模式稳定就提前停止，不稳定才追加预算。

该模块可命名为：

> **预算自适应的可信姿态模式恢复**

英文：

> **Budget-adaptive Reliable Pose Mode Recovery, BRPMR**

## 4.2 预算的含义

这里的预算不只是时间，也包括：

- 已处理参考点数量；
- 已形成场景点对数量；
- 哈希检索次数；
- 邻桶扩展次数；
- 投票候选数量；
- 几何验证次数；
- ICP 精修次数；
- 总运行时间。

预算自适应的目标是动态决定：

- 是否继续处理新参考点；
- 是否继续生成新点对；
- 是否启动邻桶检索；
- 是否做局部精修；
- 是否提前停止。

## 4.3 候选级评分

对每个候选位姿 (T_i)，定义候选质量分数：

[
S(T_i)=
w_1V_i+w_2G_i+w_3C_i-w_4A_i
]

其中：

- (V_i)：鲁棒投票分数；
- (G_i)：几何对齐质量；
- (C_i)：可见区域覆盖率；
- (A_i)：歧义惩罚，例如候选主要由高碰撞桶支撑；
- (w_1,w_2,w_3,w_4)：权重。

候选级评分回答：

> 单个候选位姿是否可靠？

## 4.4 模式级评分

将相近候选聚成姿态模式。两个候选 (T_i=(R_i,t_i)) 和 (T_j=(R_j,t_j)) 如果满足：

[
|t_i-t_j|<\tau_t
]

[
d_R(R_i,R_j)<\tau_R
]

则认为属于同一模式。

对每个模式 (M_q)，定义模式分数：

[
G(M_q)=
\eta_1\sum_{T_i\in M_q}S(T_i)
+\eta_2C_q
+\eta_3O_q
-\eta_4A_q
]

其中：

- (\sum S(T_i))：模式内部候选总支持；
- (C_q)：模式紧凑度；
- (O_q)：可见覆盖率；
- (A_q)：模式歧义惩罚；
- (\eta_1,\eta_2,\eta_3,\eta_4)：权重。

模式级评分回答：

> 一簇候选是否构成稳定可信的姿态模式？

## 4.5 模式稳定性指标

预算自适应决策不能只看最高分，还要判断当前姿态模式是否稳定。

### 4.5.1 Top-1 / Top-2 模式间隔

设当前最高模式为 (M_1)，第二模式为 (M_2)：

[
\text{Margin}=
\frac{G(M_1)-G(M_2)}{G(M_1)+\epsilon}
]

如果 margin 大，说明第一模式明显领先。

### 4.5.2 模式熵

将模式分数归一化：

[
p_q=\frac{\exp(G(M_q)/\tau)}{\sum_j\exp(G(M_j)/\tau)}
]

计算熵：

[
H=-\sum_q p_q\log p_q
]

如果熵低，说明证据集中在少数模式上。
如果熵高，说明存在多峰竞争。

### 4.5.3 模式内部紧凑度

真实姿态通常表现为多个相近候选共同支撑，而不是孤立峰值。

可以定义：

[
C_q=\exp\left(
-\frac{\sigma_t^2}{\tau_t^2}
-\frac{\sigma_R^2}{\tau_R^2}
\right)
]

其中：

- (\sigma_t^2)：模式内平移方差；
- (\sigma_R^2)：模式内旋转方差。

模式越紧凑，可信度越高。

### 4.5.4 可见区域覆盖率

如果一个模式只由局部重复结构支撑，即使投票高，也可能是假峰。

因此需要看该模式的支持点是否覆盖足够的可见区域：

[
O_q=\frac{\text{supported visible regions}}{\text{all visible regions}}
]

覆盖越充分，说明该模式不是由局部偶然结构诱导。

### 4.5.5 边际收益

边际收益是预算自适应的关键。

第 (r) 轮后，top 模式分数变化为：

[
\Delta G_r=
\frac{G_r(M_1)-G_{r-1}(M_1)}{\text{cost}_r+\epsilon}
]

如果继续处理点对后，top 模式几乎不变，说明继续计算收益很小，可以提前停止。

## 4.6 增量式模式池

为了避免“每轮只处理一批点对，后面点对无法进入前面聚类”的问题，需要维护动态模式池。

不是每一轮独立聚类，而是：

```text
第 1 轮生成候选 → 建立模式池 {M1, M2, ...}
第 2 轮生成新候选 → 分配到已有模式，或创建新模式
第 3 轮继续更新已有模式
```

每个模式保存：

```text
模式 Mq：
    代表位姿
    总分数
    候选数量
    平移方差
    旋转方差
    支持点集合
    可见覆盖率
    歧义惩罚
    最近几轮分数变化
```

新候选 (T_i) 到来后，计算它到已有模式代表位姿的距离。如果满足：

[
|t_i-\bar{t}_q|<\tau_t
]

[
d_R(R_i,\bar{R}_q)<\tau_R
]

就加入模式 (M_q)，并更新该模式的状态。否则新建一个模式。

## 4.7 模式合并与轻量重聚类

为避免早期候选较少导致模式被错误分裂，可以设计两个机制。

### 4.7.1 模式合并

如果两个模式后来变得很接近：

[
|\bar{t}_a-\bar{t}_b|<\tau_t^{merge}
]

[
d_R(\bar{R}_a,\bar{R}_b)<\tau_R^{merge}
]

则合并两个模式。

### 4.7.2 周期性轻量重聚类

每隔几轮，对当前 top-(K) 高分候选重新聚类一次，修正早期聚类误差。

注意，这里不是对所有历史候选重聚类，而是只处理高分候选，因此计算量可控。

## 4.8 自适应决策规则

每一轮处理一批参考点或点对后，更新模式池，然后进行判断。

### 情况 1：提前停止

如果满足：

[
\text{Margin}>\tau_m
]

[
H<\tau_h
]

[
C_1>\tau_c
]

[
O_1>\tau_o
]

[
\Delta G_r<\tau_g
]

则说明：

- 第一模式明显领先；
- 多峰歧义较弱；
- 模式内部紧凑；
- 可见区域覆盖充分；
- 新增点对收益很小。

此时提前停止，输出 top 模式代表位姿。

### 情况 2：继续普通点对匹配

如果模式还不稳定，但边际收益仍然较高，说明继续加入普通高价值点对仍有帮助。此时追加下一批高价值参考点或点对。

### 情况 3：启动边界选择性检索

如果模式不稳定，并且当前高价值点对中有较多跨 bin 风险，则启动 Idea 2 的不确定性感知邻桶检索。

这相当于针对当前不确定来源补充候选，而不是无差别地增加所有点对。

### 情况 4：启动局部精修

如果 top 模式明显领先，但几何误差仍偏大，可以不再生成大量新候选，而是只对 top 模式内少量候选做 ICP 或可见性验证。

## 4.9 整体流程

```text
输入：模型点云 M，场景点云 S，最大预算 Bmax

1. 根据 Idea 1 选择高价值参考点
2. 按批次生成场景点对
3. 对每批点对执行标准 PPF 检索
4. 对存在跨 bin 风险的点对执行 Idea 2 的边界选择性检索
5. 生成候选位姿并加入动态模式池

while 当前预算 < Bmax:

    6. 更新每个模式的分数、紧凑度、覆盖率和歧义惩罚
    7. 计算模式稳定性：
          Top-1/Top-2 margin
          mode entropy
          mode compactness
          visible coverage
          marginal gain

    8. 如果模式稳定且边际收益低：
          提前停止

    9. 如果模式不稳定：
          追加高价值点对
          或启动边界选择性检索
          或对 top 模式局部精修

输出：最高可信模式的代表位姿
```

## 4.10 论文表述建议

> 本文提出一种预算自适应的可信姿态模式恢复方法。本文将 PPF 后端决策由一次性最高峰选择扩展为序贯式模式恢复过程，在每一批点对投票后动态维护姿态模式池，并根据模式分数间隔、模式熵、内部紧凑度、可见覆盖率和新增证据边际收益判断当前位姿是否稳定。当最优模式已经具有充分支持且继续计算收益有限时，系统提前停止；当仍存在多峰歧义时，系统选择性追加高价值点对、边界邻桶检索或局部精修。该策略使计算预算集中于困难样本和不稳定姿态模式，从而改善 PPF 的精度—效率折中。

------

# 5. 三个 idea 的关系

三个 idea 不是孤立模块，而是一条连续的效率优化链。

## 5.1 Idea 1：减少进入系统的低价值点对

作用位置：点对生成前。

解决问题：

> 不要让大量低判别、重复和噪声点对进入后续 PPF 检索。

主要效果：

- 减少场景参考点数量；
- 减少场景点对数量；
- 减少哈希查询次数；
- 减少无效候选；
- 减少伪投票。

## 5.2 Idea 2：只对存在量化风险的点对补查

作用位置：哈希检索阶段。

解决问题：

> 正确匹配可能因为固定量化边界而漏检，但不能对所有点对全量增强检索。

主要效果：

- 保留量化边界补偿能力；
- 避免全量多表检索；
- 在较小额外成本下提高候选召回；
- 降低无效邻桶检索。

## 5.3 Idea 3：姿态模式稳定后提前停止

作用位置：候选生成和后端决策阶段。

解决问题：

> 不同样本难度不同，不应该所有样本都使用相同计算量。

主要效果：

- 简单样本快速停止；
- 困难样本继续追加预算；
- 避免无意义后续匹配和精修；
- 提高平均效率。

------

# 6. 最终贡献点写法

## 贡献 1：歧义感知的高价值点对选择

> 提出基于 PPF 哈希判别力的场景参考点与点对选择方法，从模型哈希桶碰撞统计中估计场景点对的潜在匹配歧义，并结合局部法向稳定性、点对多样性和空间冗余约束，在点对生成阶段过滤低判别和重复点对。

## 贡献 2：不确定性感知的边界选择性检索

> 提出一种不确定性感知的边界选择性候选检索机制，根据连续 PPF 特征到量化边界的距离与特征不确定性的关系判断是否存在跨 bin 风险，仅对必要维度执行有限邻桶扩展，从而替代全量多分辨率随机检索。

## 贡献 3：预算自适应的可信姿态模式恢复

> 提出序贯式姿态模式恢复与预算控制策略，动态维护可更新的姿态模式池，并根据模式间隔、模式熵、内部紧凑度、可见覆盖率和边际收益判断是否提前停止、追加点对、启动边界检索或局部精修。

------

# 7. 方法框架命名建议

建议整体方法名称：

> **歧义感知与预算自适应的高效 PPF 位姿配准方法**

英文：

> **Ambiguity-aware and Budget-adaptive Efficient PPF Registration**

三个模块名称：

| 模块   | 中文名称                   | 英文名称                                     | 缩写  |
| ------ | -------------------------- | -------------------------------------------- | ----- |
| Idea 1 | 歧义感知场景点对选择       | Ambiguity-aware Scene Pair Selection         | ASPS  |
| Idea 2 | 不确定性感知边界选择性检索 | Uncertainty-aware Boundary Selective Probing | UBSP  |
| Idea 3 | 预算自适应可信姿态模式恢复 | Budget-adaptive Reliable Pose Mode Recovery  | BRPMR |

整体 pipeline：

```text
模型点云 M，场景点云 S

离线：
1. 构建模型 PPF 哈希表
2. 统计哈希桶碰撞度与判别力

在线：
1. ASPS：选择高价值参考点和场景点对
2. 标准 PPF 单桶检索
3. UBSP：仅对跨 bin 风险点对执行邻桶检索
4. 生成候选位姿并投票
5. BRPMR：增量更新姿态模式池
6. 根据模式稳定性和边际收益进行预算控制
7. 输出最高可信姿态模式代表位姿
```

------

# 8. 实验组织

实验上不需要单独对比“新方法”和“初稿 RS-MRQ”。初稿 RS-MRQ 不是公开发表方法，也不是审稿人关注的正式 baseline。更清晰的实验逻辑是：

> **与已有 PPF/位姿估计方法做整体对比，对最终方法做自身消融，并补充效率统计。**

建议实验章节按以下结构组织：

```text
4.1 数据集与评价指标
4.2 对比方法与实验设置
4.3 与现有 PPF 方法的整体对比
4.4 消融实验
    4.4.1 高价值点对选择的有效性
    4.4.2 不确定性感知边界检索的有效性
    4.4.3 预算自适应模式恢复的有效性
4.5 效率分析
4.6 参数敏感性分析
4.7 失败案例与讨论
```

## 8.1 实验一：整体对比实验

### 目的

证明最终方法相比已有 PPF 或位姿估计方法，在精度和时间之间取得更好的折中。

### 对比方法

可以选择：

| 方法类别       | 方法示例                               |
| -------------- | -------------------------------------- |
| 经典 PPF       | Drost PPF                              |
| 改进 PPF       | PPT 或相关论文中已有的 PPF 改进方法    |
| 后端验证类方法 | 带重评分、可见性验证或 ICP 的 PPF 方法 |
| 本文方法       | Ours                                   |

不一定需要重点对比大量深度学习方法。因为论文定位是几何式 PPF 改进，重点是 CAD 已知、弱纹理、训练数据有限场景。可以少量列学习式方法作为参考，但主对比应放在 PPF 系列方法上。

### 评价指标

| 指标              | 含义               |
| ----------------- | ------------------ |
| ADD / ADD-S       | 位姿估计精度       |
| SR / Recall       | 成功率或召回率     |
| Rotation Error    | 平均旋转误差       |
| Translation Error | 平均平移误差       |
| Time              | 平均运行时间       |
| FPS               | 可选，表示在线效率 |

### 表格模板

| 方法         | ADD/ADD-S ↑ | SR ↑ | 旋转误差 ↓ | 平移误差 ↓ | 平均时间 ↓ |
| ------------ | ----------- | ---- | ---------- | ---------- | ---------- |
| Drost PPF    |             |      |            |            |            |
| PPF Method A |             |      |            |            |            |
| PPF Method B |             |      |            |            |            |
| Ours         |             |      |            |            |            |

### 预期结论

> 本文方法不是追求绝对最快，而是在相近或更低时间成本下获得更稳定的位姿估计结果，改善 PPF 方法的精度—效率折中。

## 8.2 实验二：核心模块消融实验

### 目的

证明三个模块分别有效。

### 消融设置

| 实验版本       | ASPS 高价值点对 | UBSP 边界选择性检索 | BRPMR 预算自适应恢复 | 目的                   |
| -------------- | --------------- | ------------------- | -------------------- | ---------------------- |
| Baseline PPF   | ×               | ×                   | ×                    | 最基础基线             |
| Ours w/o ASPS  | ×               | √                   | √                    | 验证高价值点对选择作用 |
| Ours w/o UBSP  | √               | ×                   | √                    | 验证边界检索作用       |
| Ours w/o BRPMR | √               | √                   | ×                    | 验证预算自适应作用     |
| Ours Full      | √               | √                   | √                    | 完整方法               |

### 表格模板

| 方法           | ADD/ADD-S ↑ | SR ↑ | 点对数量 ↓ | 候选数量 ↓ | 平均时间 ↓ |
| -------------- | ----------- | ---- | ---------- | ---------- | ---------- |
| Baseline PPF   |             |      |            |            |            |
| Ours w/o ASPS  |             |      |            |            |            |
| Ours w/o UBSP  |             |      |            |            |            |
| Ours w/o BRPMR |             |      |            |            |            |
| Ours Full      |             |      |            |            |            |

### 预期结论

- 去掉 ASPS：点对数量和候选数量明显增加，时间变长，伪投票可能增加；
- 去掉 UBSP：量化边界附近的正确匹配召回下降，遮挡和噪声场景精度下降；
- 去掉 BRPMR：最终精度可能接近，但平均时间明显增加；
- 完整方法：在精度和效率上取得最好折中。

## 8.3 实验三：ASPS 高价值点对选择消融

### 目的

证明 ASPS 的效果不是来自简单减少点数，而是来自更有判别力的点对选择。

### 对比设置

| 方法                      | 说明                 |
| ------------------------- | -------------------- |
| Random Sampling           | 随机选参考点         |
| Uniform Sampling          | 均匀下采样           |
| Curvature-based Sampling  | 基于曲率选择参考点   |
| Normal-stability Sampling | 只基于法向稳定性选择 |
| ASPS                      | PPF 哈希歧义感知选择 |

### 表格模板

| 采样策略         | 参考点数量 ↓ | 点对数量 ↓ | 平均桶判别力 ↑ | SR ↑ | 时间 ↓ |
| ---------------- | ------------ | ---------- | -------------- | ---- | ------ |
| Random           |              |            |                |      |        |
| Uniform          |              |            |                |      |        |
| Curvature        |              |            |                |      |        |
| Normal-stability |              |            |                |      |        |
| ASPS             |              |            |                |      |        |

### 预期结论

> ASPS 在相同参考点数量下能够产生更高判别力的点对，减少高碰撞桶查询和伪投票，因此比随机、均匀或单纯曲率采样更稳定。

## 8.4 实验四：UBSP 边界选择性检索消融

### 目的

证明“不确定性感知”比单纯边界距离更合理，同时证明选择性检索比全量增强检索更高效。

### 对比设置

| 方法                        | 说明                    |
| --------------------------- | ----------------------- |
| Single-bin                  | 只查当前桶              |
| Boundary-only               | 只根据边界距离查邻桶    |
| Uncertainty-only            | 只根据不确定性判断      |
| UBSP                        | 边界距离 + 特征不确定性 |
| UBSP + Ambiguity Constraint | 加入高歧义桶限制        |

### 表格模板

| 检索策略                    | 匹配召回 ↑ | 邻桶触发比例 ↓ | 返回模型点对数 ↓ | SR ↑ | 时间 ↓ |
| --------------------------- | ---------- | -------------- | ---------------- | ---- | ------ |
| Single-bin                  |            |                |                  |      |        |
| Boundary-only               |            |                |                  |      |        |
| Uncertainty-only            |            |                |                  |      |        |
| UBSP                        |            |                |                  |      |        |
| UBSP + Ambiguity Constraint |            |                |                  |      |        |

### 预期结论

> UBSP 能够在较低邻桶触发比例下提高边界风险点对的匹配召回，并避免对所有点对执行全量增强检索。

## 8.5 实验五：BRPMR 预算自适应恢复消融

### 目的

证明预算自适应机制能减少平均计算时间，同时不明显损害精度。

### 对比设置

| 方法                | 说明                               |
| ------------------- | ---------------------------------- |
| No Early Stop       | 不早停，处理完整预算               |
| Fixed Budget        | 固定处理一定比例点对               |
| Score-only Stop     | 只根据 top-1 分数停止              |
| Mode-stability Stop | 根据模式稳定性停止                 |
| BRPMR Full          | 模式稳定性 + 边际收益 + 动态模式池 |

### 表格模板

| 策略                | 使用点对比例 ↓ | 早停比例 ↑ | SR ↑ | 平均时间 ↓ |
| ------------------- | -------------- | ---------- | ---- | ---------- |
| No Early Stop       |                |            |      |            |
| Fixed Budget        |                |            |      |            |
| Score-only Stop     |                |            |      |            |
| Mode-stability Stop |                |            |      |            |
| BRPMR Full          |                |            |      |            |

### 预期结论

> 仅根据 top-1 分数停止可能误判多峰歧义，而 BRPMR 结合模式间隔、模式熵、内部紧凑度、可见覆盖率和边际收益，可以更可靠地判断当前结果是否已经稳定。

## 8.6 实验六：效率统计实验

### 目的

直接回应“是否只是用更多时间换精度”的问题。

### 统计内容

建议统计：

- 场景参考点数；
- 场景点对数；
- 哈希查询次数；
- 邻桶扩展次数；
- 返回模型点对数量；
- 生成候选位姿数量；
- 进入后端验证候选数量；
- 平均时间。

### 表格模板

| 方法           | 参考点数 ↓ | 场景点对数 ↓ | 哈希查询数 ↓ | 邻桶扩展数 ↓ | 模型点对返回数 ↓ | 候选数 ↓ | 后端验证数 ↓ | 时间 ↓ |
| -------------- | ---------- | ------------ | ------------ | ------------ | ---------------- | -------- | ------------ | ------ |
| Baseline PPF   |            |              |              |              |                  |          |              |        |
| Ours w/o ASPS  |            |              |              |              |                  |          |              |        |
| Ours w/o UBSP  |            |              |              |              |                  |          |              |        |
| Ours w/o BRPMR |            |              |              |              |                  |          |              |        |
| Ours Full      |            |              |              |              |                  |          |              |        |

### 预期结论

> 完整方法能够显著减少点对数量、哈希查询次数、候选位姿数量和后端验证次数，说明时间降低来自有效计算控制，而不是偶然实现差异。

## 8.7 实验七：参数敏感性分析

### 目的

证明方法对关键参数不敏感，并给出合理参数设置。

### 建议分析参数

| 参数      | 含义                       |
| --------- | -------------------------- |
| (M)       | 高价值参考点数量           |
| (K)       | 每个参考点预采样配对点数量 |
| (\lambda) | UBSP 边界风险安全系数      |
| (m)       | 最大扩展维度数             |
| (\tau_m)  | 模式间隔阈值               |
| (\tau_h)  | 模式熵阈值                 |
| (\tau_g)  | 边际收益阈值               |

### 表格/曲线建议

可以画：

- (M) 对精度和时间的影响；
- (\lambda) 对邻桶触发比例、精度和时间的影响；
- (m) 对匹配召回和时间的影响；
- 早停阈值对精度和时间的影响。

### 预期结论

> 在较宽参数范围内，方法均能保持稳定性能，说明其效果不是由极端参数调优得到。

## 8.8 实验八：失败案例与讨论

### 目的

增加论文可信度，说明方法边界。

可以分析以下失败情况：

- 目标可见区域过少，无法形成稳定模式；
- 目标高度对称，多个模式都合理；
- 场景法向估计严重错误；
- 模型与实际物体存在几何偏差；
- 高价值点对选择过强，导致少数关键点对被过滤。

讨论时可以说明未来改进方向：

- 引入可见性先验；
- 融合颜色或边缘信息；
- 对对称物体采用对称等价评价；
- 使用更稳定的法向估计；
- 将预算控制与实例分割结果结合。

------

# 9. 最小完整实验组合

如果时间有限，建议至少完成以下四组实验。

## 实验 1：整体对比

与 PPT 或论文中已有 PPF 方法比精度、误差和时间。

## 实验 2：模块消融

比较：

- Baseline PPF
- Ours w/o ASPS
- Ours w/o UBSP
- Ours w/o BRPMR
- Ours Full

## 实验 3：效率统计

统计：

- 点对数量；
- 哈希查询次数；
- 邻桶扩展次数；
- 候选数量；
- 后端验证数量；
- 平均时间。

## 实验 4：关键参数分析

至少分析：

- 高价值参考点数量 (M)；
- UBSP 阈值 (\lambda)；
- 最大扩展维度数 (m)；
- 预算自适应停止阈值。

这四组实验已经可以支撑一篇完整论文。

------

# 10. 摘要核心句建议

可以在摘要中使用如下表述：

> 现有 PPF 改进方法通常通过增强描述子、扩大候选检索或复杂后端验证提升鲁棒性，但容易带来额外计算开销。本文认为，复杂场景下 PPF 的关键并非生成更多候选，而是识别高判别点对、选择性补偿量化边界误差，并在姿态模式稳定时及时停止无效计算。为此，本文提出一种歧义感知与预算自适应的高效 PPF 位姿估计框架，包括基于 PPF 哈希判别力的高价值点对选择、不确定性感知的边界选择性检索，以及预算自适应的可信姿态模式恢复。该框架在减少低价值匹配和伪投票的同时，将计算预算集中分配给高不确定性样本，从而改善 PPF 方法的精度—效率折中。

------

# 11. 一句话总结

> 本文方法的核心不是“用更多计算换更高精度”，而是“用更有效的计算提升精度”。
> **ASPS 少生成低价值点对，UBSP 只补查有跨 bin 风险的点对，BRPMR 在姿态模式稳定后提前停止。**